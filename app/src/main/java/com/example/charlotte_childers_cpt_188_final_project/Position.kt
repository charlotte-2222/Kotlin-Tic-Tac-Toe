package com.example.charlotte_childers_cpt_188_final_project

data class Position(val row: Int, val column: Int) // row and column are zero-indexed
// row is the row number, column is the column number (left to right) of the position on the board